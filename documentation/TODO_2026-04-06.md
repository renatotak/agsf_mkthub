# TODO — 2026-04-06

> **Reconciled 2026-04-28** against [`ROADMAP.md`](../ROADMAP.md). Original wording preserved; strikethroughs mark shipped items, ⚠️ marks partials, ❌ marks gaps. **New backlog items (2026-04-28)** added inline per module. **Wave 3 sprint 2026-04-30** shipped 5 more items. See **Remaining gaps** at the bottom.

---

## Painel

- [x] ~~First row of indicators can open a modal to highlight what is important in each chapter; in modal, show a button with a link to the chapter~~ — **Done** (Phases 1b/1c, `ChapterModal` pattern across Industries / Scrapers KPIs)
- ⚠️ Mapa de Inteligência Integrada: ~~highlight news, events,~~ and weather ~~on map (parsed with location when scraped); map filter by date range~~ — **Mostly done** via Phase 3 (subsidiary + news-attached markers, 90d window, layer toggles, date/UF/city filters). **Gap: weather markers not on map.**
- [x] ~~Notícias in Painel parsed to improve knowledge base; news from other sources (LLM agents cron jobs)~~ — **Done** (Wave 3). `sync-llm-news-scout` weekly cron: Cheerio scrapes Canal Rural, Globo Rural, Embrapa, CNA, AgroLink; Gemini batches 10 articles/call for PT-BR summaries (max 5 calls/run, 50 articles/run); inline `extractNormsFromNews` + `matchEntities`. 5 new source-registry entries.
- [x] ~~Create a webapp version of whole app, with chat to KB; if necessary RAG + MCP server~~ — **Done** (Next.js webapp; persistent Oracle chat — Phase 6a, tier-aware; 9-tool MCP server `npm run mcp`; `/api/knowledge/chat` RAG)
- [x] ~~**🔔 Bell notification icon not working**~~ — **Done 2026-04-30** (Phase 29 Wave 1). Bell now opens dropdown fed by `/api/activity?limit=10` with outside-click handler + "Ver todas" CTA to Settings.
- [x] ~~**Briefing Executivo — persona-based tabs**~~ — **Done** (Wave 3). `persona_briefings` table (mig 079) + `/api/persona-briefing` lazy-generation endpoint (Vertex AI, cached 24h). `ExecutiveBriefingWidget.tsx` now has 5 pill tabs: Geral | CEO | Comercial | Crédito | Marketing. Generated on first tab-click, not at cron time — zero added daily cost.

## Pulso de Mercado

- ⚠️ ~~Connect to OECD, FAO, USDA studies; Brazilian exports volume, Brazilian production, world production~~ — **Mostly done.** Shipped: FAOSTAT, FAOSTAT livestock, CONAB safra, USDA PSD, MDIC ComexStat (BR exports), WB Pink Sheet. **Gap: no OECD source.**

## Inteligência de Insumos

- [x] ~~List of all products registered in Brazil; molecule ↔ brand ↔ company; oracle UX for alternatives~~ — **Done** (Wave 3). Phase 5: 800 AGROFIT products + 40 AMIS canonical inputs + Pacote/Indústria/Mapa tabs + SVG mindmap. `sync-adapar-parana` weekly cron: ADAPAR Paraná PDF (~3,500 products) → `industry_products` with `source_dataset='adapar_parana'` (mig 078, partial UNIQUE index for state registries).

## Radar Competitivo

- [x] ~~CRUD modal + web search insights per company~~ — **Done** (CRUD existed; Phase 4a added URL paste → Cheerio + Vertex AI auto-categorize)

## Notícias Agro

- [x] ~~Modal/list of news providers with CRUD~~ — **Done** (DataSources.tsx + Source CRUD UI, Phase 25)
- [x] ~~Reading-room Chrome extension connected to webapp instead of localhost~~ — **Done** (Phase 22 + recent `pull-from-mkthub` + add/delete proxy hardening, commits 878cd40 + 2ef06e6)
- [x] ~~Missing events (baldebranco, agroadvance)~~ — **Done** (`sync-events-agroadvance` weekly + `sync-events-agrural` Phase 2d)
- [x] ~~Source-of-information detail + button for agent to gather more info from event sites~~ — **Done** (Phase 4d: event URL paste → AI parse + LocationConfirmModal)
- [x] ~~**Update button not working**~~ — **Done 2026-04-30** (Phase 29 Wave 1). `handleRefresh` now POSTs to new `/api/news-na/refresh` (calls `runSyncAgroNews` server-side, bypasses the `CRON_SECRET` gate cleanly), with spinner + toast feedback.

## Diretório de Canais

1. ⚠️ ~~Highlight RJ companies + companies expanding operations (recently opened CNPJs)~~ — **Partial.** RJ cross-ref via `v_retailers_in_rj` ✅. **Gap: Expansion Detection alerts** still in optional backlog, blocked on `CRAWLERS_DATABASE_URL`.
2. [x] ~~Split Indústrias into separate chapter~~ — **Done** (Diretório de Indústrias · 274 industries · 1,699 establishments)
3. [x] ~~More KPI cards (categories bar, cities, RJ count, news count modals)~~ — **Done** (Phase 1d curation chips + KPI summary endpoints + RJ/news modals — Phase 24G)
4. ⚠️ ~~Inpev flag, POS pictures via Google Maps, AgriSafe OneNote import~~ — **Mostly done.** `StreetViewTile` per matriz (Phase 24G slice 3) ✅. OneNote imports + Settings → Reclassificar Importações (Phase 6f) ✅. **Gap: Inpev integration never added.**
5. [x] ~~CRM tool with 3 confidentiality tiers; KB respects classification; schedule meetings + find leads + connect to Central de Conteúdo~~ — **Done** (`EntityCrmPanel` Pessoas/Reuniões/Pipeline — Phase 24G; `src/lib/confidentiality.ts` tier infra; tier-aware chat — Phase 24G slice 1; ContentHub suggestion engine — Phase 6b). **Note: `client_confidential` tier + RBAC** in optional backlog (low ROI single-user).
6. [ ] **Revise all OneNote files (Renato + Davi)** — audit imported OneNote content for classification, data quality, and duplicates. *(New 2026-04-28)*

## Marco Regulatório

- [x] ~~Button to insert documents of new laws/regulations + modal with list of main sources~~ — **Done** (`/api/regulatory/upload` Phase 24C + Source CRUD Phase 25 + Resumo Regulatório + "Atualizar Agora" Phase 6d)
- [x] ~~**Improve regulatory document type indicators**~~ — **Done 2026-04-30** (Phase 29 Wave 1). New `src/data/regulatory-doc-types.ts` registry covers 21 norm types with bilingual labels, color, icon. `DocTypeBadge` + multi-select chip filter with per-type counts in `RegulatoryFramework.tsx`.

## Recuperação Judicial

- [x] ~~Manual CNPJ insert flow~~ — **Done** (`/api/rj-add` Phase 24C: BrasilAPI lookup + DDG debt scrape)
- [x] ~~Debt amount scraped from process or news~~ — **Done** (debt_value_source chip — Phase 6e).
- [x] ~~**RJ analysis via Receita Federal + Notícias Agro**~~ — **Done** (Phase 29 + Wave 3). `rj_candidates` table (mig 077) + `sync-rj-candidates` weekly cron + `/api/rj-candidates` GET/PATCH. "Candidatos" review tab in `RecuperacaoJudicial.tsx` (Wave 3): pending-count badge, keyword badges, news snippet preview, Rejeitar / Promover→RJ buttons with pre-filled CNPJ.

## Ingestão de Dados

- [x] ~~User can add new sources~~ — **Done** (`data_sources` table + Source CRUD UI — Phase 25)
- [x] ~~UX/UI to see what info is being used + which tables enriched~~ — **Done** (Scraper Health tab + `ActivityLogPanel` in Settings — Phase 24G2, ~100% write-path coverage)

## Central de Conteúdo

- [ ] **Major content overhaul** — current state is outdated. Feed from LinkedIn published posts (Renato's); LinkedIn newspaper feature; Instagram posts; automated content creation based on the knowledge base being built in this project. Large feature package — needs own spec/phasing. *(New 2026-04-28)*

## Log de Reuniões

- [x] ~~**Company name matching issues**~~ — **Done** (Wave 3). `src/lib/meeting-entity-matcher.ts`: 3-stage algo matcher (tax-ID exact → ILIKE bidirectional → in-process trigram similarity, threshold 0.4). `entity_match_confidence` column on `crm_meetings` (mig 080): `auto`/`needs_review`/`no_match`/`manual`. `/api/crm/meetings/rematch` POST endpoint. `MeetingsLog.tsx`: "Corrigir Vínculos" button, ⚠️ per-entity review badge, per-meeting confidence indicator.

## Instituições Financeiras

- [x] ~~**Delinquency & defaults charts tab**~~ — **Done 2026-04-30** (Phase 29 Wave 1). New "Inadimplência" tab in `FinancialInstitutions.tsx` with 5 BCB SGS series (rural PJ/PF + system-wide PF/PJ/total benchmarks), 12/24/36/all period selector, KPI cards with delta vs 12mo. `sync-bcb-scr-inadimplencia` extended from 3→5 series + history extended to 60 months.

---

## Remaining gaps

After Wave 3 (sprint 2026-04-30 cont.):

1. ~~**Weather markers on Painel map**~~ — **Done.** 25 agro-hub cities served by `/api/map/markers/weather` (1h ISR, Embrapa ClimAPI), `MarkerType.weather` layer toggle in `DashboardMap.tsx`.
2. ~~**LLM-agent news cron jobs**~~ — **Done** (Wave 3). `sync-llm-news-scout` weekly, 5 portals, Cheerio + Gemini batch, 50 articles/run max, 5 API calls/run max.
3. ~~**OECD as `macro_statistics` source**~~ — **Done.** `sync-oecd` job pulls OECD-FAO Agricultural Outlook (SDMX-CSV, 9 countries × 8 commodities × 4 measures × 8 years ≈ ~2,000 rows/run). Mig 076.
4. ~~**State-level ag-input registries**~~ — **Done** (Wave 3). `sync-adapar-parana` weekly: ADAPAR Paraná PDF → `industry_products` (`source_dataset='adapar_parana'`, mig 078). SP still blocked (JSF form). Other states deferred.
5. **Inpev flag on retailer cards** — still pending (no public Inpev source identified yet).
6. ~~**Briefing Executivo persona-based tabs**~~ — **Done** (Wave 3). Lazy Vertex AI generation, `persona_briefings` table, 5 tabs in `ExecutiveBriefingWidget.tsx`.
7. ~~**Meeting log company name matching**~~ — **Done** (Wave 3). `meeting-entity-matcher.ts`, `entity_match_confidence` column, "Corrigir Vínculos" UI button.
8. **Central de Conteúdo overhaul** — large feature package, needs own spec/phasing.
9. **OneNote audit (Renato + Davi)** — manual content review, not a code task.

## Documented blockers (optional backlog)

- **Expansion Detection alerts** — diff on `cnpj_establishments` for new branches → daily themed briefing. Blocked on `CRAWLERS_DATABASE_URL` access.
- ~~**Serasa RJ backfill**~~ — **Abandoned 2026-04-28.** No Serasa CSV with RJ companies exists. Replaced by RF + Notícias Agro approach (see Recuperação Judicial section above).
- **CRM RBAC + `client_confidential` tier** — infra exists; needs `user_profiles` + tier-filter on `/api/crm/*` reads + Settings UI. Low ROI single-user.
