Painel:
First row of indicators can open a modal to highlight what is important in each chapter; in modal, show a button with a link to the chapter
Mapa de Inteligência Integrada: it is important to highlight news, events, and weather on map (therefore, all these features must natively always be parsed with location when scraped or inserted into database); map must also have a filter by date range (next 30 days, next 90 days) in events filter
Noticias in Painel show be parsed to improve knowledge base; also, news from other sources (like LLM agents cron jobs (claude tasks, grok tasks) can be implemented, because app will run in cloud, but it will always be managed and controlled by user in a desktop
Create a webapp version of whole app, which will also have a message feature for user to talk to agent that gets data from knowledge base (if necessary, create a RAG and MCP server to improve UX and conversation)

Pulso de mercado will always be opened by users that want to check and get insights by region or by culture of commodities market data. All this information must be connected to other information, more stale, like OECD, FAO, and USDA studies (those crawlers are not implemented yet); map does not need a filter by culture, since all information below the first filter will reflect the culture itself (map and widget must adhere to the main filter); in this chapter now it is missing other information with higher latency, like Brazilian exports volume, Brazilian production, world production, and other information easily scraped from oecd, fao, worldbank, and other information providers (update ingestao de dados also)

Inteligencia de insumos is a chapter that needs to be implemented from status quo. First task is to gather a list with all products registered to be used in Brazil; those lists are available publicly, either by national government, and by each state; list has the relationship of molecules (active principle) with brand name and company that produces it (thus allowing to enrich database, knowledge base and other features); these chapter needs a list of public sources, a first batch of data to be scraped and new UX to act as an oracle of data to provide to user alternatives to patented products most used by rural producers, in order to improve rural producers of a given culture and region a more efficient ag input package to acquire

Radar Competitivo needs a modal with a CRUD function so that user can add or delete a given competitor; also, user may want to add information about each company, and have more insights via web search

Notícias Agro must have a modal or list with all news provider available, so that user can CRUD it. I have a chrome extension in my computer that also collects important news that can be added to this chapter. Revise reading-room (C:\Users\renat\.gemini\antigravity\projects\1 personal\reading-room) extension to connect with this application, instead of saving it in my localhost. There are some events missing (https://baldebranco.com.br/confira-os-grandes-eventos-do-agro-em-2026/ and https://agroadvance.com.br/blog-feiras-e-eventos-agro-2026/); provide more details of source of information, and add a button to allow agent to gather more information from websites about events; events will be important information to feed to other AgriSafe services like App Campo, a mobile application that will assist field sales executives in organizing their agenda and clients


Diretorio de Canais:
1 - Based on main news portals like Noticias Agricolas, Agribiz, neofeed, Bloomberglinea/agro, globo rural and other sources (that can be edited - CRUD in ingestao de dados chapter), this chapter needs to highlight not only main companies in Recuperação Judicial, but also companies that are expanding operations (query in Receita Federal - cnpj tables to find recently opened cnpjs in a given region)
2 - Industrias will be another chapter (split information about industrias in a new chapter called Diretorio de Industrias); this chapter will have the same UX UI, but focused on industries and tradings instead of retailers or cooperatives
3 - Main indicators can show more indicators (first box with Total Canais and a smaller horizontal bar chart showing amount by category), another box showing the amount of cities with channels, showing a smaller bar chart with concentration in top cities, another box showing the number of channels in recuperacao judicial, (then with a modal to highlight all that is already showing in this chapter about it), and one last box with number of channels that appeared in any news portal (with a modal to show what company, what portal, and when it was published)
4 - Many information needs to be added to aech company, like if company is listed in Inpev data, picture of the POS using google maps feature, AgriSafe data that will be imported from onenote files with meetings, key persons, interests of various companies
5 - AgriSafe will use this feature as its CRM tool; therefore, information will have at least 3 layers of confidentiality: public domain; AgriSafe proprietary data to be published; AgriSafe confidential data (there will be one last layer which is confidential data from companies also, in the future); all this information will need a modal and tools to enrich database, and connection with knowledge base in accordance with confidentiality classification. AgriSafe will also use whis tool to schedule meetings and find leads (and therefore connect information to Central de Conteúdo, to send newsletter to WhatsApp groups and e-mails)

Marco Regulatório:
 - Provide a button for the user to insert documents of new laws and regulations; a modal with the list of main sources

Recuperação Judicial:
 - Many companies in Recuperação Judicial are missing, however it is hard for the user to analyse and insert new cnpj to be included and analysed
 - Debt amount is an information that needs to be scraped from the judicial process or from news (using duckduckgo or google)

Ingestão de dados:
 - List is already good, user may want to add new sources
 - It is still difficult for user to analyse what information is being used or not, if information was used to enrich database (which tables). UX and UI need revision