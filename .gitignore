# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db

# env files (catch-all for .env, .env.local, .env.macro, .env.development, etc.)
.env
.env.*

# data files
*.xlsx

# merged project archives
merged projects data/
agsf_mkthub-main/

# local-only debris dump (cleanup-YYYY-MM-DD/ batches go here)
tmp/

# GCP service account keys (NEVER commit)
agrisafe-*.json

# user-saved local files (CSVs, PDFs, JSON exports — uploaded to Supabase as
# needed, not committed because they may contain sensitive data and are large)
local files/

# Claude Code session state (worktree admin, settings, transcripts)
.claude/
